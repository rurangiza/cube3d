<!-- - colors: multiple commas -->
- character after the map: return error

- check we have East, West, North, South

- check espace texture et couleur (key value pair, split, bon nombre)
- presence d'un player ()

switch (current_pos) {
    < prev_start:
        // error
    > prev_end:
        // error
    < next_start:
        // error
    > next_end:
        // error
}

creer deux listes, 

// traverse all the rows and for each one, do the following
    // store previous row blocks (start/end), if any, in a doubly-linked list
        // count number of blocks
        // add each block as a node in the linked list
    // store next row blocks (start/end), if any, in a doubly-linked list
    // traverse the current row and for each one, do the following
        // skips spaces
        // comprare current position with previous and next line blocks (moving through the linked-list)
            // if missmatch, return error
    // free both linked lists


// Utilities
+ creating linked lists
+ manipulating them
+ freing them

- creating a node
- adding node at end of linked list
- delete all nodes in linked list
